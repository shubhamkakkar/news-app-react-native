todo = {
    make a new saga and make a new redu for loadCustom news and inthisprops.newNews wold be ussed to render the  items and not the tthis.props.topHealnes news add some logic hat  i lodCstom news from searchContainer is triggered then 
    profile sections : {
        ui of profile section
        place a human button on header intead of making a drawer
        set default search query, category and country --- atleast one of it
        saveButton {
                default set from defaultSelection list should be displayed in the 
                profile section with obviou option to update it there as well

                saveButton will send this to redux and async storage,
                naming of asyncStorage item setItem(""), is in the splash screen
        }
    }
}

further todo = {
    maintain a record o searched query in the async storage for suggestion purposes
    a cross button to remove it from there only -- remove from async as well
    option in profile to maintian the search history -- could be updated
    depending on the searchHistory save boolean, add logic in splashscreen to fetch from async storage or not

}